import java.util.*;

public class DbController {
    /*private Books books;
    private Dorosli dorosli;
    private Dzieci dzieci;
    HashMap<Integer, Dorosli> dor_hmap = new HashMap<Integer, Dorosli>();
    HashMap<Integer, Dzieci> dz_hmap = new HashMap<Integer, Dzieci>();

    public DbController(Books books, Dorosli dorosli, Dzieci dzieci){
        this.books = books;
        this.dorosli = dorosli;
        this.dzieci = dzieci;
    }

    public void add_to_dbase(ArrayList<String> book_atributes){
        if(books instanceof dorosli){

        } else if(books instanceof dzieci){

        }
    }*/

}